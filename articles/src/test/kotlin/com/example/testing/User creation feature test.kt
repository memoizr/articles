package com.example.testing

import org.junit.Test

class `User creation feature test` {
    @Test
    fun `Displays success screen when creating a user with all fields valid`() {
    }

    @Test
    fun `Does not send request when creating a user with any field invalid`() {
    }

    @Test
    fun `Sends latest data when creating a user after view is reattached`() {
    }

    @Test
    fun `Shows error when creating a user with no valid email`() {
    }

    @Test
    fun `Shows error when creating a user with no valid username`() {
    }

    @Test
    fun `Shows error when creating a user and requests fails`() {
    }

    @Test
    fun `Sends request when creating a user with all fields valid`() {
    }
}